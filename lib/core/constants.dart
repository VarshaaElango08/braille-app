import 'package:flutter/material.dart';

class AppText {
  static const appName = "Science Voice Learning";
}

class AppColors {
  static const primary = Color(0xFF1E5AA8);
  static const bg = Color(0xFFF6F7FB); // ✅ fixes bg error
  static const card = Colors.white;
  static const border = Color(0xFFE6E8EF);
}
